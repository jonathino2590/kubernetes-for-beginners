apiVersion: v1
kind: Pod
metadata:
  name: nginx-2
  labels:
    app: myapp
    tier: frontend
spec:
  containers:
    - name: nginx 
      image: nginx
      imagePullPolicy: IfNotPresent
      ports:
        - name: http
          containerPort: 80
          protocol: TCP